# Core data processing and numerical computation
numpy>=1.24.0
pandas>=2.0.0

# Machine learning and model evaluation
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# APK analysis
androguard>=3.4.0

# File handling and system operations (usually included in Python standard library, but listed for completeness)
python-dateutil>=2.8.2  # Often used with pandas

# Logging and subprocess (part of standard library, but ensuring compatibility)
# No additional packages needed beyond standard library

# Optional: For JSON handling in preprocessing (part of standard library, but listed for clarity)
# No additional packages needed

# Optional: For pickle operations (part of standard library)
# No additional packages needed

